{
    "id": "8cbbcc37-f8d1-4d36-96fb-61e46714d2b8",
    "name": "PipelinePackerInstaller",
    "friendlyName": "Pipeline Packer tool installer",
    "description": "Find in cache or download a specific version of Packer and prepend it to the PATH",
    "author": "sethbacon",
    "helpMarkDown": "[Learn more about this task](https://github.com/sethbacon/azure-pipelines-packer)",
    "category": "Tool",
    "visibility": [
        "Build",
        "Release"
    ],
    "demands": [],
    "version": {
        "Major": "1",
        "Minor": "8",
        "Patch": "0"
    },
    "instanceNameFormat": "Install Packer $(packerVersion)",
    "inputs": [
        {
            "name": "packerVersion",
            "type": "string",
            "label": "Version",
            "defaultValue": "latest",
            "required": true,
            "helpMarkDown": "The version of Packer to install. Use 'latest' to install the current latest release."
        },
        {
            "name": "downloadSource",
            "type": "pickList",
            "label": "Download source",
            "defaultValue": "hashicorp",
            "required": true,
            "helpMarkDown": "Where to download the Packer binary from.",
            "options": {
                "hashicorp": "HashiCorp official releases (releases.hashicorp.com)",
                "registry": "Private registry (terraform-registry-backend)",
                "mirror": "Custom mirror URL"
            }
        },
        {
            "name": "registryUrl",
            "type": "string",
            "label": "Registry base URL",
            "defaultValue": "",
            "required": true,
            "visibleRule": "downloadSource = registry",
            "helpMarkDown": "Base URL of your terraform-registry-backend instance. Must use HTTPS. Example: https://registry.example.com"
        },
        {
            "name": "registryMirrorName",
            "type": "string",
            "label": "Mirror configuration name",
            "defaultValue": "packer",
            "required": true,
            "visibleRule": "downloadSource = registry",
            "helpMarkDown": "The mirror configuration name in your registry backend. This is the {name} path segment in /terraform/binaries/{name}/..."
        },
        {
            "name": "registryAllowedHosts",
            "type": "string",
            "label": "Allowed registry download hosts",
            "defaultValue": "",
            "required": false,
            "visibleRule": "downloadSource = registry",
            "helpMarkDown": "Optional comma- or newline-separated allowlist of the storage hosts the registry may hand the binary download off to. A `*.` prefix matches subdomains only (e.g. `*.s3.amazonaws.com`). The allowlist is enforced on the initial download URL and re-checked on every redirect hop. When empty (default), the registry may name any public host, but a host that is — or resolves to — a loopback, link-local/metadata (169.254.0.0/16), carrier-grade-NAT, or RFC1918/ULA private address is refused; set this to pin a registry that legitimately serves from a private or air-gapped storage host."
        },
        {
            "name": "mirrorBaseUrl",
            "type": "string",
            "label": "Mirror base URL",
            "defaultValue": "",
            "required": true,
            "visibleRule": "downloadSource = mirror",
            "helpMarkDown": "Base URL of your binary mirror. Must use HTTPS. Must serve files at the same path structure as releases.hashicorp.com/packer. Example: https://artifacts.example.com/hashicorp/packer"
        },
        {
            "name": "mirrorAllowedHosts",
            "type": "string",
            "label": "Allowed mirror hosts",
            "defaultValue": "",
            "required": false,
            "visibleRule": "downloadSource = mirror",
            "helpMarkDown": "Optional comma- or newline-separated host allowlist for private or air-gapped mirrors. A `*.` prefix matches subdomains only. The allowlist is enforced on the initial download URL and re-checked on every redirect hop. When empty (default), a host that is — or resolves to — a loopback, link-local/metadata (169.254.0.0/16), carrier-grade-NAT, or RFC1918/ULA private address is refused, on the initial URL and on every redirect hop alike. Addresses are classified numerically, so short-form/decimal/octal/hex IPv4 and IPv4-mapped IPv6 spellings are covered. Note that the default-deny check resolves DNS at check time and does not pin the resulting address into the connection, so it is defense-in-depth against a host that statically points at a private address rather than a complete defense against active DNS rebinding."
        },
        {
            "name": "requireGpgSignature",
            "type": "boolean",
            "label": "Require GPG signature verification",
            "defaultValue": "true",
            "required": false,
            "visibleRule": "downloadSource != registry",
            "helpMarkDown": "When enabled, fail if the GPG signature file is unavailable. Defaults to true for HashiCorp downloads. Disable for mirrors that do not serve .sig files."
        },
        {
            "name": "requireChecksum",
            "type": "boolean",
            "label": "Require checksum verification",
            "defaultValue": "true",
            "required": false,
            "visibleRule": "downloadSource != hashicorp",
            "helpMarkDown": "When enabled (default), fail if the mirror or registry does not provide a SHA256 checksum for the requested version. Disable only if your mirror/registry cannot publish checksums and you trust it to verify server-side."
        }
    ],
    "restrictions": {
        "commands": {
            "mode": "restricted"
        },
        "settableVariables": {
            "allowed": [
                "packerLocation",
                "packerDownloadedFrom",
                "PATH"
            ]
        }
    },
    "execution": {
        "Node20_1": {
            "target": "src/index.js"
        },
        "Node24": {
            "target": "src/index.js"
        }
    },
    "outputVariables": [
        {
            "name": "packerLocation",
            "description": "The location of the packer binary that was installed on the agent."
        },
        {
            "name": "packerDownloadedFrom",
            "description": "The source from which packer was downloaded: 'hashicorp', 'registry:<url>', 'mirror:<url>', or 'cache'."
        }
    ],
    "messages": {
        "VerifyPackerInstallation": "Verifying Packer installation...",
        "InputVersionNotValidSemanticVersion": "Input version %s is not a valid semantic version",
        "PackerNotFoundInFolder": "Packer executable not found in path %s",
        "OperatingSystemNotSupported": "Operating system %s is not supported",
        "ArchitectureNotSupported": "Architecture %s is not supported",
        "PackerDownloadFailed": "Failed to download Packer from url %s. Error: %s",
        "PackerVersionNotFound": "Unable to get latest version from source, using fallback version",
        "GettingLatestPackerVersion": "Getting latest Packer version.",
        "InsecureUrlRejected": "Insecure URL rejected: %s. Only HTTPS URLs are allowed.",
        "MirrorDownloadHostIsPrivate": "Mirror download host %s is private or link-local and was rejected. Configure mirrorAllowedHosts to explicitly allow a trusted private mirror.",
        "MirrorDownloadHostNotAllowed": "Mirror download host %s is not in mirrorAllowedHosts (%s).",
        "RegistryDownloadHostIsPrivate": "Registry download host %s is private or link-local and was rejected. Configure registryAllowedHosts to explicitly allow a trusted private storage host.",
        "RegistryDownloadHostNotAllowed": "Registry download host %s is not in registryAllowedHosts (%s).",
        "RegistryRequestFailed": "Registry request failed for %s. HTTP status: %s",
        "Sha256VerificationFailed": "SHA256 verification failed. Expected: %s, Got: %s",
        "ResolvingLatestFromRegistry": "Resolving latest Packer version from registry: %s",
        "ResolvedVersionFromRegistry": "Resolved latest version from registry: %s"
    }
}